export type User = {
  userName: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
};
export type Customer = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  startDate: string;
  endDate: string;
};
export type Coach = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
};
export type Login = {
  username: string;
  password: string;
};
